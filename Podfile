# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'nav_bar_color_transition' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for nav_bar_color_transition

  target 'nav_bar_color_transitionTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'KMNavigationBarTransition'
  end

  target 'nav_bar_color_transitionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
